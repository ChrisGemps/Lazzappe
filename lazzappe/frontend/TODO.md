# TODO for Dashboard Navigation and Login State

- [ ] Update NavBarComponent.js to add redirects for Sign Up and Login buttons using Link to /register and /login
- [ ] Add state management in NavBarComponent to check login status from localStorage (username)
- [ ] Conditionally render: if logged in, show user's name and a cart button; else show Sign Up and Login buttons
- [ ] Update LoginPage.js to ensure it sets localStorage on login (already does, but confirm)
- [ ] Test navigation and conditional display
